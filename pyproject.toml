[tool.ruff]
target-version = "py313"  # Using Python 3.13.3 as specified in your project

[tool.ruff.lint]
select = ["E", "F", "I"]  # pycodestyle, pyflakes, import sorting

[tool.ruff.format]
# Black-compatible formatting (default settings)